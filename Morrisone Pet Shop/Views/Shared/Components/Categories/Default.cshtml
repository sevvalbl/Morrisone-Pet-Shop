@model IEnumerable<Category>

@* <div class ="card mb-4">
<div class="card-header">Kategoriler</div>    
<div class="card-body">
    <div class="row">
        <div class="col-sm-6">
            <ul class="list-unstyled mb-0">
                    @foreach(var item in Model)
                    {
                        <li><a href="/Categories/Index/@item.Id">@item.Name</a></li>
                    }
              
            </ul>
        </div>
    </div>
</div>
</div> *@
<h1></h1>

<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-lg-4">
            <div class="card mb-4" >
                <a href="#!"><img class="card-img-top" src="/ProjectImages/@item.Image" alt="" /></a>
                <div class="card-body">
                    

                    <a class="btn btn-primary" href="/Categories/Index/@item.Id">@item.Name</a>
                </div>
            </div>
        </div>
    }
</div>